# Nastya Portfolio

Современное портфолио продуктового дизайнера Anastasia Ivanova, построенное на React + Vite + TypeScript с Tailwind CSS и Framer Motion анимациями.

## Технологический стек

- **React 18** — библиотека для построения пользовательских интерфейсов
- **TypeScript** — типизированный JavaScript
- **Vite** — быстрый сборщик и dev-сервер
- **Tailwind CSS** — утилитарный CSS-фреймворк
- **Framer Motion** — анимации и переходы
- **React Router DOM** — роутинг
- **Lucide React** — иконки
- **Bun** — быстрый пакетный менеджер

## Начало работы

### Предварительные требования

Убедитесь, что у вас установлен Bun:

```bash
# Windows (PowerShell)
powershell -c "irm bun.sh/install.ps1|iex"
```

### Установка

```bash
# Клонируйте репозиторий и перейдите в папку
cd nastya-portfolio

# Установите зависимости
bun install
```

### Шрифты

Загрузите шрифты Evolventa и Suisse Intl в папку `src/fonts/`:
- Evolventa-Regular.woff2, Evolventa-Bold.woff2
- SuisseIntl-Regular.woff2, SuisseIntl-Medium.woff2, SuisseIntl-Bold.woff2

### Запуск

```bash
# Запуск dev-сервера
bun run dev
```

Приложение откроется на `http://localhost:3000`

### Сборка

```bash
# Сборка для продакшена
bun run build

# Предпросмотр сборки
bun run preview
```

## Структура проекта

```
src/
├── components/
│   ├── layout/
│   │   ├── Header.tsx          # Шапка с меню
│   │   ├── Footer.tsx          # Подвал
│   │   └── Layout.tsx          # Общий layout
│   ├── ui/
│   │   ├── Button.tsx          # Кнопка
│   │   ├── CustomCursor.tsx    # Кастомный курсор
│   │   └── MusicWidget.tsx     # Виджет музыки
│   ├── home/
│   │   ├── HeroSection.tsx     # Hero секция
│   │   ├── MarqueeStrip.tsx    # Бегущая строка
│   │   └── ProjectsGrid.tsx    # Сетка проектов
│   └── project/
│       ├── ProjectCard.tsx     # Карточка проекта
│       └── ContentRenderer.tsx # Рендер блоков контента
├── pages/
│   ├── HomePage.tsx            # Главная страница
│   └── ProjectPage.tsx         # Страница кейса
├── data/
│   └── projects.ts             # Данные проектов
├── hooks/
│   └── useMousePosition.ts     # Хук для курсора
├── types/
│   └── index.ts                # TypeScript типы
├── fonts/                      # Шрифты (загрузить вручную)
├── assets/                     # Изображения проектов
├── App.tsx                     # Главный компонент с роутингом
├── main.tsx                    # Точка входа
└── index.css                   # Глобальные стили + Tailwind
```

## Особенности

- ✨ **Кастомный курсор** — круглый курсор с анимацией при наведении
- 🎵 **Music Widget** — имитация виджета Яндекс.Музыки
- 🎬 **Page Transitions** — плавные переходы между страницами
- 📱 **Адаптивный дизайн** — Mobile First подход
- 🚀 **Framer Motion** — анимации появления элементов
- 🎨 **Минималистичный дизайн** — чистая типографика, много воздуха

## Добавление проектов

Проекты описаны в `src/data/projects.ts`. Каждый проект имеет:

```typescript
{
  id: 'project-slug',           // URL slug
  title: 'Название проекта',
  category: 'Fintech' | 'Mobile' | 'Web' | 'Identity',
  thumbnailUrl: importedImage,   // Превью для сетки
  shortDescription: 'Краткое описание',
  fullDescription: 'Полное описание',
  coverImage: importedImage,     // Обложка на странице кейса
  content: [                     // Блоки контента
    { type: 'image', content: {...} },
    { type: 'text', content: {...} },
    { type: 'gallery', content: {...} },
    { type: 'stats', content: {...} },
  ]
}
```

## Лицензия

MIT
